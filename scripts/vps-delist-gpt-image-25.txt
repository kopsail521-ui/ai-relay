# 只下架 OpenLux：gpt-image-2.5-flare-c / gpt-image-2.5-sunburst-c
# 保留 Grsai：gpt-image-2.5 / flare / sunburst
cd /opt/ai-relay && sudo git pull && git log -1 --oneline

sudo python3 scripts/vps-delist-gpt-image-25.py /opt/ai-relay/data/new-api/one-api.db

sudo docker restart new-api 2>/dev/null || true
sleep 3
curl -sS http://127.0.0.1:3001/api/pricing | python3 -c "
import sys,json
blob=json.dumps(json.load(sys.stdin),ensure_ascii=False)
for mid in ['gpt-image-2.5','gpt-image-2.5-flare','gpt-image-2.5-sunburst','gpt-image-2.5-flare-c','gpt-image-2.5-sunburst-c']:
  print(mid, 'IN' if mid in blob else 'OUT')
"
echo DONE_DELIST_GPT_IMAGE_25_C
