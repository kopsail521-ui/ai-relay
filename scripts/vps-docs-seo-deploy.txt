# VPS：DeepSeek free 回表（flash-free / pro-free / Prover）
# 前提：本机已 git push
set -e
cd /opt/ai-relay
sudo git fetch origin
sudo git checkout -- static/seo config/seo || true
sudo git pull --ff-only origin main
sudo bash scripts/deploy-brand-static.sh
echo "--- smoke ---"
curl -sS https://www.keyoapi.xyz/free-models | grep -oE 'deepseek-v4-flash-free|deepseek-v4-pro-free|DeepSeek-Prover-V2-7B|Family.*DeepSeek' | sort -u
curl -sS https://www.keyoapi.xyz/model/deepseek-v4-flash | grep -oE 'Free Tier|Free tier available|deepseek-v4-flash-free' | sort -u
curl -sS https://www.keyoapi.xyz/model/deepseek-v4-pro | grep -oE 'Free Tier|Free tier available|deepseek-v4-pro-free' | sort -u
git -C /opt/ai-relay rev-parse --short HEAD
