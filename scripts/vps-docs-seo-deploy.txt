# VPS：清死 free twin + 0731 进表 + 稳定 lastmod
cd /opt/ai-relay
sudo git fetch origin
sudo git stash push -u -m "vps-wip" || true
sudo git pull --ff-only origin main
sudo bash scripts/deploy-brand-static.sh
echo "--- smoke ---"
curl -sS https://www.keyoapi.xyz/deepseek-api-pricing | grep -oE 'flash-free|pro-free|DeepSeek-Prover-V2-7B' | sort -u
curl -sS https://www.keyoapi.xyz/model/deepseek-v4-flash | grep -oE 'Free Tier|Pricing' | head -3
curl -sS https://www.keyoapi.xyz/pricing-list | grep -o 'deepseek-v4-flash-0731' | head -1
curl -sS https://www.keyoapi.xyz/sitemap.xml | grep -oE '<lastmod>[^<]+' | sort -u
git -C /opt/ai-relay rev-parse --short HEAD
