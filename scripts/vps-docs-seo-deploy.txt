# VPS：pricing-list DeepSeek 拆行（v4.1-flash $0.18 / v4-flash $1.10）
# 前提：本机已 git push
set -e
cd /opt/ai-relay
sudo git fetch origin
sudo git checkout -- static/seo config/seo || true
sudo git pull --ff-only origin main
sudo bash scripts/deploy-brand-static.sh
echo "--- smoke ---"
curl -sS https://www.keyoapi.xyz/pricing-list | grep -oE 'deepseek-v4\.1-flash|deepseek-v4-flash|\$0\.18 / \$0\.72|\$1\.10 / \$3\.30' | sort | uniq -c
curl -sS https://www.keyoapi.xyz/model/deepseek-v4.1-flash | grep -oE '\$0\.18 / \$0\.72|not an automatic upgrade|Separate' | sort -u
curl -sS https://www.keyoapi.xyz/model/deepseek-v4-flash | grep -oE '\$1\.10 / \$3\.30|\$0\.18 / \$0\.72 \(v4\.1' | sort -u
git -C /opt/ai-relay rev-parse --short HEAD
