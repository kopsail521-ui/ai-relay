# 一步一粘贴（每步等回车完成再贴下一步；避免 bash xxx.txt 超长断连）

# ①
cd /opt/ai-relay && sudo git pull --ff-only origin main && ls scripts/vps-fix-bespoke-jev-desc.py && echo OK_PULL

# ② 改广场 DB 简介
sudo docker run --rm -v /opt/ai-relay/data/new-api:/data -v /opt/ai-relay/scripts/vps-fix-bespoke-jev-desc.py:/tmp/fix.py:ro python:3.12-alpine python /tmp/fix.py /data/one-api.db

# ③ 热更新 moderation 简介 i18n（容器名按实际，常见 ai-relay-creem-moderation）
sudo docker cp /opt/ai-relay/config/marketplace-model-copy.json ai-relay-creem-moderation:/app/marketplace-model-copy.json && sudo docker restart ai-relay-creem-moderation && echo OK_COPY

# ④ 品牌页 + Caddy（稍长，可单独跑；断了重连后再贴一次）
cd /opt/ai-relay && sudo bash scripts/deploy-brand-static.sh

# ⑤ 冒烟
curl -sS https://www.keyoapi.xyz/ | grep -o 'Jev / Open-Jev' | head -1; echo DONE_BESPOKE_JEV
