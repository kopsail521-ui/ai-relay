# Deprecated standalone patch — use durable SEO deploy instead:
#   node scripts/print-vps-seo-caddy.mjs
# Then paste scripts/vps-seo-deploy-one-liner.txt on Aliyun Workbench.
#
# That one-liner syncs static/seo + writes full Caddyfile with:
#   /robots.txt /sitemap.xml / /compare /model/* /brand/* before SPA proxy
#
# Do NOT run install scripts that tee a Caddyfile without SEO handles.

echo "Use: node scripts/print-vps-seo-caddy.mjs"
exit 1
